---
- name: Converge
  hosts: localhost
  connection: local
  gather_facts: no
  collections:
    - name: community.kubernetes
      version: "<1.0.0"

  tasks:
    - name: Create Namespace
      k8s:
        api_version: v1
        kind: Namespace
        name: '{{ namespace }}'

    - import_tasks: kustomize.yml
      vars:
        state: present
